pyyaml>=6.0
numpy>=1.21.0
# Phase 3 RL dependencies
# Use gymnasium (modern gym API); stable-baselines3 >= 2.x requires it.
# If you have an older environment, gymnasium >= 0.26.0 is compatible.
gymnasium>=0.26.0
stable-baselines3>=2.0.0
